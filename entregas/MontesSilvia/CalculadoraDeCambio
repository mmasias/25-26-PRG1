import java.util.Scanner;
class CalculadoraDeCambio {
    public static void main (String[]args) {
        Scanner scanner =new Scanner(System.in);
        System.out.println ("¿Cuál es la cantidad de dinero a pagar?");
        int dineroPorPagar=scanner.nextInt();
        System.out.println("¿Cuánto dinero es entregado?");
        int dineroEntregado=scanner.nextInt();
        int dineroPorDevolver=dineroEntregado -dineroPorPagar;
        int billetes200= dineroPorDevolver/200;
        dineroPorDevolver = dineroPorDevolver % 200;
        int billetes100= dineroPorDevolver/100;
       dineroPorDevolver=cdineroPorDevolver %100;
        int billetes50=dineroPorDevolver /50;
        dineroPorDevolver= dineroPorDevolver %50;
        int billetes20= dineroPorDevolver/ 20;
        dineroPorDevolver=dineroPorDevolver %20;
        int billetes10 =dineroPorDevolver/10;
        dineroPorDevolver=dineroPorDevolver %10;
        int billetes5=dineroPorDevolver/5;
        dineroPorDevolver=dineroPorDevolver %5;
        int monedas2=dineroPorDevolver/2;
        dineroPorDevolver=dineroPorDevolver %2;
        int monedas1=dineroPorDevolver/1;
        dineroPorDevolver=dineroPorDevolver %1;
        String salida;
        salida = "Se devuelven";
        salida=billetesDe200> 0 ? "y"+ billetesDe200+ "billete(s) de 200" : "";
        salida=salida+(billetes100>0? "y" +billetes100 +"billete(s) de 100": "");
        salida=salida+(billetes50>0 ? "y" + billetes50+ "billete(s) de 50": "");
        salida=salida+(billetes20>0 ? "y" + billetes20+ "billete(s) de 20": "");
        salida=salida+(billetes10>0 ? "y" + billetes10+ "billete(s) de 10": "");
        salida=salida+(monedas2>0 ? "y" + monedas2+ "moneda(s) de 2": "");
        salida =salida +(dineroPorDevolver>0 ? "y" +dineroPorDevolver+ "billete(s) de 1\n ": "");
        System.out.println(salida);
    
        
    


    }
}
